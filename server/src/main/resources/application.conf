ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }

    application {
        modules = [ com.vocabmaxxing.ApplicationKt.module ]
    }
}

database {
    url = "jdbc:postgresql://localhost:5432/vocabmaxxing"
    url = ${?DATABASE_URL}
    user = "postgres"
    user = ${?DATABASE_USER}
    password = "postgres123"
    password = ${?DATABASE_PASSWORD}
}

openai {
    apiKey = ""
    apiKey = ${?OPENAI_API_KEY}
}
