ktor {
    deployment {
        port = 8080
    }

    application {
        modules = [ com.vocabmaxxing.ApplicationKt.module ]
    }
}

database {
    url = "jdbc:postgresql://localhost:5432/vocabmaxxing"
    user = "postgres"
    password = "postgres123"
}

jwt {
    secret = "your-jwt-secret"
    issuer = "your-jwt-issuer"
    audience = "your-jwt-audience"
    realm = "your-jwt-realm"
}

openai {
    apiKey = "your-your-api-key"
}
